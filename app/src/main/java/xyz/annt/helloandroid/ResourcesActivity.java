package xyz.annt.helloandroid;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class ResourcesActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_resources);

//        ImageView image = (ImageView) findViewById(R.id.image_clip);
//        ClipDrawable drawable = (ClipDrawable) image.getDrawable();
//        drawable.setLevel(drawable.getLevel() + 7000);

//        ImageView image = (ImageView) findViewById(R.id.image_transition);
//        TransitionDrawable drawable = (TransitionDrawable) image.getDrawable();
//        drawable.startTransition(1000);

//        ImageView image = (ImageView) findViewById(R.id.signal_cellular);
//        image.setImageLevel(3);
    }
}
